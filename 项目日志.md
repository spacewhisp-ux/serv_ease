# Serv Ease 项目日志

## 2026-04-23 后端功能增强

### 1. 修复用户回复已关闭工单的问题

**问题描述：**
用户可以回复已关闭的工单，并且工单会自动重新打开。这与业务需求不符。

**修复内容：**
- 在 `TicketsService.replyTicket` 方法中添加状态检查
- 如果工单状态为 CLOSED，抛出 BadRequestException
- 移除了自动重新打开工单的逻辑

**修改文件：**
- [server/src/tickets/tickets.service.ts](server/src/tickets/tickets.service.ts)

**验证结果：**
- ✅ 用户无法回复已关闭的工单
- ✅ 返回错误信息："Closed tickets cannot be replied to"

---

### 2. 实现工单关键词搜索功能

**功能描述：**
支持管理员通过关键词搜索工单，搜索范围包括：
- 工单号（ticketNo）
- 工单主题（subject）
- 用户邮箱（user.email）

**实现内容：**
- 在 `AdminListTicketsDto` 中添加 `keyword` 字段
- 在 `AdminService.listTickets` 方法中实现搜索逻辑
- 使用 PostgreSQL 的不区分大小写搜索（mode: 'insensitive'）

**修改文件：**
- [server/src/admin/dto/admin-list-tickets.dto.ts](server/src/admin/dto/admin-list-tickets.dto.ts)
- [server/src/admin/admin.service.ts](server/src/admin/admin.service.ts)

**API 使用示例：**
```bash
GET /v1/admin/tickets?keyword=SE-20260423
GET /v1/admin/tickets?keyword=Test
GET /v1/admin/tickets?keyword=test@example.com
```

---

### 3. 实现优先级和分类筛选功能

**功能描述：**
支持管理员按优先级和分类筛选工单列表。

**实现内容：**
- 在 `AdminListTicketsDto` 中添加 `priority` 和 `category` 字段
- 在 `AdminService.listTickets` 方法中添加筛选逻辑

**修改文件：**
- [server/src/admin/dto/admin-list-tickets.dto.ts](server/src/admin/dto/admin-list-tickets.dto.ts)
- [server/src/admin/admin.service.ts](server/src/admin/admin.service.ts)

**API 使用示例：**
```bash
GET /v1/admin/tickets?priority=HIGH
GET /v1/admin/tickets?category=GENERAL
GET /v1/admin/tickets?priority=URGENT&category=TECHNICAL
```

---

### 4. 实现工单操作历史功能

**功能描述：**
记录工单的所有操作历史，包括创建、状态变更、分配、回复、关闭等操作。

**数据库变更：**
- 创建 `ticket_histories` 表
- 添加 `TicketHistoryAction` 枚举（CREATED, STATUS_CHANGED, ASSIGNED, REASSIGNED, PRIORITY_CHANGED, CATEGORY_CHANGED, REPLIED, CLOSED, REOPENED）
- 添加 `TicketHistoryActorRole` 枚举（USER, AGENT, ADMIN, SYSTEM）

**实现内容：**
- 创建 `TicketHistoryService` 服务
- 在以下操作中记录历史：
  - 用户创建工单（CREATED）
  - 用户回复工单（REPLIED）
  - 用户关闭工单（CLOSED）
  - 管理员分配工单（ASSIGNED/REASSIGNED）
  - 管理员回复工单（REPLIED，仅公开回复）
  - 管理员更新工单状态（STATUS_CHANGED）
- 添加 API 接口 `GET /v1/admin/tickets/:id/history`

**新增文件：**
- [server/src/tickets/ticket-history.service.ts](server/src/tickets/ticket-history.service.ts)
- [server/prisma/migrations/20260423_add_ticket_history/migration.sql](server/prisma/migrations/20260423_add_ticket_history/migration.sql)

**修改文件：**
- [server/prisma/schema.prisma](server/prisma/schema.prisma)
- [server/src/tickets/tickets.module.ts](server/src/tickets/tickets.module.ts)
- [server/src/tickets/tickets.service.ts](server/src/tickets/tickets.service.ts)
- [server/src/admin/admin.module.ts](server/src/admin/admin.module.ts)
- [server/src/admin/admin.service.ts](server/src/admin/admin.service.ts)
- [server/src/admin/admin.controller.ts](server/src/admin/admin.controller.ts)

**API 使用示例：**
```bash
GET /v1/admin/tickets/:id/history
```

**返回数据结构：**
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "action": "STATUS_CHANGED",
      "actorRole": "ADMIN",
      "oldValue": "OPEN",
      "newValue": "IN_PROGRESS",
      "createdAt": "2026-04-23T03:46:25.301Z",
      "actor": {
        "id": "uuid",
        "displayName": "Admin User",
        "email": "admin@example.com",
        "role": "ADMIN"
      }
    }
  ]
}
```

---

### 5. 验证测试

**测试脚本：**
创建了完整的 API 验证脚本 [server/test-api.sh](server/test-api.sh)，测试内容包括：

1. ✅ 用户注册和登录
2. ✅ 创建工单
3. ✅ 列出工单
4. ✅ 获取工单详情
5. ✅ 管理员登录
6. ✅ 列出可分配的客服
7. ✅ 分配工单给客服
8. ✅ 管理员公开回复
9. ✅ 管理员内部备注
10. ✅ 用户回复工单
11. ✅ 管理员更新工单状态
12. ✅ 非法状态流转验证
13. ✅ 用户关闭工单
14. ✅ 验证已关闭工单不能回复
15. ✅ 验证已关闭工单不能分配

---

## 总结

本次更新完成了以下主要功能：

1. **Bug 修复**：修复了用户可以回复已关闭工单的问题
2. **搜索功能**：实现了工单关键词搜索
3. **筛选功能**：实现了优先级和分类筛选
4. **历史记录**：实现了完整的工单操作历史追踪

所有功能均已通过验证测试，后端构建成功。

---

## 2026-04-23 Admin Web 前端功能增强

### 1. 工单列表页面增强

**功能描述：**
在工单列表页面添加关键词搜索、优先级筛选和分类筛选功能。

**实现内容：**

#### 1.1 关键词搜索
- 添加搜索输入框，支持搜索工单号、主题、用户邮箱
- 使用 Ant Design 的 Input 组件，支持清空功能
- 搜索参数通过 `keyword` 字段传递给后端 API

#### 1.2 优先级筛选
- 添加优先级下拉选择框
- 支持 LOW、NORMAL、HIGH、URGENT 四个优先级
- 使用不同颜色的 Tag 展示优先级

#### 1.3 分类筛选
- 添加分类下拉选择框
- 支持 GENERAL、TECHNICAL、BILLING、FEATURE_REQUEST、BUG_REPORT、OTHER 六个分类

**修改文件：**
- [admin-web/src/modules/tickets/api.ts](admin-web/src/modules/tickets/api.ts)
  - 更新 `TicketListQuery` 接口，添加 `keyword`、`priority`、`category` 字段
  - 添加 `ticketPriorityLabels` 标签映射
- [admin-web/src/modules/tickets/TicketListPage.tsx](admin-web/src/modules/tickets/TicketListPage.tsx)
  - 更新 `FilterValues` 接口
  - 添加关键词搜索输入框
  - 添加优先级筛选下拉框
  - 添加分类筛选下拉框
  - 更新查询逻辑

**UI 效果：**
```
┌─────────────────────────────────────────────────────────────────┐
│ [Search by ticket no, subject, or email] [Status] [Priority]   │
│ [Category] [All tickets] [Filter] [Reset]                       │
└─────────────────────────────────────────────────────────────────┘
```

---

### 2. 工单详情页面增强

**功能描述：**
在工单详情页面添加操作历史展示功能。

**实现内容：**

#### 2.1 操作历史展示
- 添加 History 卡片，展示工单的所有操作历史
- 使用 Ant Design 的 Timeline 组件展示历史记录
- 每条历史记录显示：
  - 操作类型（Created、Status Changed、Assigned 等）
  - 操作者角色（USER、AGENT、ADMIN、SYSTEM）
  - 操作者信息
  - 变更前后的值（如果有）
  - 操作时间

#### 2.2 历史记录类型
支持以下操作类型的展示：
- CREATED - 工单创建
- STATUS_CHANGED - 状态变更
- ASSIGNED - 分配工单
- REASSIGNED - 重新分配
- PRIORITY_CHANGED - 优先级变更
- CATEGORY_CHANGED - 分类变更
- REPLIED - 回复
- CLOSED - 关闭
- REOPENED - 重新打开

**修改文件：**
- [admin-web/src/modules/tickets/api.ts](admin-web/src/modules/tickets/api.ts)
  - 添加 `TicketHistoryAction` 类型
  - 添加 `TicketHistoryActorRole` 类型
  - 添加 `TicketHistory` 接口
  - 添加 `ticketHistoryActionLabels` 标签映射
  - 添加 `getHistory` API 方法
- [admin-web/src/modules/tickets/TicketDetailPage.tsx](admin-web/src/modules/tickets/TicketDetailPage.tsx)
  - 添加 `HistoryItem` 组件
  - 添加 `historyQuery` 查询
  - 添加 History 卡片展示
  - 在 `invalidateTicket` 中添加历史记录的刷新

**UI 效果：**
```
┌─ History ─────────────────────────────────────────────────────┐
│ ● [ADMIN] Status Changed  "OPEN" → "IN_PROGRESS"              │
│   by Admin User  2026-04-23 11:46:25                          │
│ ● [USER] Replied  by Test User  2026-04-23 11:30:15          │
│ ● [ADMIN] Assigned  by Admin User  2026-04-23 11:20:10       │
│ ● [USER] Created  by Test User  2026-04-23 11:00:00          │
└───────────────────────────────────────────────────────────────┘
```

---

### 3. 验证测试

**构建验证：**
```bash
cd admin-web && npm run build
```

**验证结果：**
- ✅ TypeScript 编译通过
- ✅ Vite 构建成功
- ✅ 无类型错误
- ⚠️ Chunk size warning（可选优化项）

---

## 总结

本次更新完成了以下主要功能：

### 后端（Server）
1. **Bug 修复**：修复了用户可以回复已关闭工单的问题
2. **搜索功能**：实现了工单关键词搜索
3. **筛选功能**：实现了优先级和分类筛选
4. **历史记录**：实现了完整的工单操作历史追踪

### 前端（Admin Web）
1. **搜索 UI**：实现了关键词搜索输入框
2. **筛选 UI**：实现了优先级和分类筛选下拉框
3. **历史展示**：实现了操作历史时间线展示

所有功能均已通过验证测试，前后端构建成功。
